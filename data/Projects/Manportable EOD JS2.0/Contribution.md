- Primary developer of a real-time, safety-critical **Explosive Ordnance Disposal (EOD) robot** — tracked base, 7-DOF arm, integrated disrupter, and a redundant wired/wireless control link — designing and implementing most of the system across the **full stack from embedded firmware to operator HMI**
- Developed a **custom CANopen (CiA-402) control library** driving **11 servo drives on two 1 Mbit/s CAN buses** in position, velocity, and torque modes — cyclic SDO setpoints at 50 Hz with interrupt-driven TPDO telemetry, plus differential-joint, lead-screw, and worm-gear drivetrain modeling.
- Wrote **STM32F103 firmware** that transparently tunnels the SBUS RC stream over a WIZnet-W5500 UDP link (UART-DMA idle-line framing, interrupt-driven sockets, PHY-loss recovery) with an automatic **hardware failover to a wireless backup** on wired-link loss.
- Derived and shipped a **closed-form inverse-kinematics solver** for a telescoping 7-DOF arm (2R shoulder/elbow + additive wrist-pitch model), verified against the URDF, integrated with **MoveIt 2** for collision-validity checks and home-pose planning.
- Engineered a **layered fail-safe system** for a weaponized platform: asymmetric per-bus fault isolation with full-disarm, latched safe states, a loopback degraded-operation mode, an encoder-battery startup gate, and fail-safe disrupter fire-control that disarms the instant the operator link drops.
- Built the **operator console** (Android/Kotlin): 5-feed low-latency RTSP switching (hardware MediaCodec), a real-time telemetry dashboard (Jetpack Compose), a **3D digital-twin OpenGL overlay**, two-way echo-cancelled intercom, gimbal control, and the fire-control UI.

